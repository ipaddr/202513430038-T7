<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- PRODUCTION: For release builds, restrict cleartext to specific domains -->
    <!-- For debug builds, the debug variant will override this with permissive settings -->

    <!-- Option 1: Allow cleartext for local development IPs (uncomment and update as needed) -->
    <!-- <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config> -->

    <!-- Option 2: Allow all cleartext (less secure, only for development) -->
    <base-config cleartextTrafficPermitted="true" />

    <!-- RECOMMENDATION: For production release, set cleartextTrafficPermitted="false" and use HTTPS -->
</network-security-config>
